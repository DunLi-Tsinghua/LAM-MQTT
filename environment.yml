name: lam-mqtt
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - pip
  - pip:
      - pandas>=2.0
      - numpy>=1.24
      - scikit-learn>=1.3
      - requests>=2.31
      - tqdm>=4.66
      - joblib>=1.3
      - pyarrow>=12
